/** 都道府県LP「口コミが多い／評判が高い」に載せる最大件数 */
export const PREFECTURE_LANDING_HIGHLIGHT_LIMIT = 6;

/** 「評判が高い」ブロックに載せる最低口コミ数（平均のノイズ除去） */
export const PREFECTURE_LANDING_MIN_REVIEWS_FOR_RATING = 3;

/** 上位ピックアップ用に一度に読み込む学校数（都道府県内の上限想定内） */
export const PREFECTURE_LANDING_FETCH_CAP = 80;

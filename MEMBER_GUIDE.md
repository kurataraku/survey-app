# メンバー向け作業ガイド

このガイドは、GitHubとCursorを使ってUI改善作業を行うメンバー向けの完全ガイドです。

## 目次

1. [前提条件](#前提条件)
2. [初回セットアップ](#初回セットアップ)
3. [日常的な作業フロー](#日常的な作業フロー)
4. [Cursorでの作業手順](#cursorでの作業手順)
5. [PR作成手順](#pr作成手順)
6. [よくある質問とトラブルシューティング](#よくある質問とトラブルシューティング)

## 前提条件

作業を始める前に、以下が準備できているか確認してください：

- [ ] GitHubアカウントを持っている
- [ ] Cursorをインストールしている
- [ ] Node.jsがインストールされている（v18以上推奨）
- [ ] Gitがインストールされている
- [ ] オーナーからリポジトリへの招待を受け取っている

### Node.jsのインストール確認

**Node.jsがインストールされているか確認：**

1. **ターミナルを開く**
   - Windows: `Win + R` → `cmd` と入力してEnter

2. **以下のコマンドを実行**
   ```bash
   node --version
   npm --version
   ```

3. **バージョンが表示されない場合**
   - Node.jsがインストールされていません
   - 以下の手順でインストールしてください

**Node.jsのインストール手順：**

1. **Node.jsの公式サイトにアクセス**
   - https://nodejs.org/ にアクセス

2. **LTS版（推奨版）をダウンロード**
   - 「推奨版」と表示されているボタンをクリック
   - Windows用のインストーラー（`.msi`ファイル）をダウンロード

3. **インストーラーを実行**
   - ダウンロードしたファイルをダブルクリック
   - インストールウィザードに従ってインストール
   - 「Add to PATH」オプションがチェックされていることを確認（通常はデフォルトでチェックされています）

4. **インストールを確認**
   - **重要**: インストール後、ターミナルを**完全に閉じて再起動**してください
   - 新しいターミナルで以下を実行：
     ```bash
     node --version
     npm --version
     ```
   - 両方のコマンドでバージョンが表示されればOK

**注意**: Node.jsをインストールした後は、必ずターミナルを再起動してください。再起動しないと、`npm`コマンドが認識されない場合があります。

## 初回セットアップ

### ステップ1: リポジトリのクローン

1. **ターミナル（コマンドプロンプト）を開く**
   - Windows: `Win + R` → `cmd` と入力してEnter
   - または、Cursorのターミナルを使用

2. **作業用のフォルダに移動**
   ```bash
   cd C:\Users\あなたのユーザー名\Documents
   ```
   （任意の場所でOK）

3. **リポジトリをクローン**
   ```bash
   git clone https://github.com/kurataraku/survey-app.git
   ```

4. **プロジェクトフォルダに移動**
   ```bash
   cd survey-app
   ```

### ステップ2: 環境変数の設定

1. **Cursorでプロジェクトを開く**
   - Cursorを起動
   - 「File」→「Open Folder」
   - `survey-app`フォルダを選択

2. **`.env.example`を`.env.local`にコピー**

   **重要**: `.env.example`は隠しファイルのため、表示されていない場合があります。

   **方法A: Cursorでコピー（推奨）**
   
   1. Cursorのエクスプローラーで、プロジェクトルート（`survey-app`フォルダ）を確認
   2. `.env.example`が表示されない場合：
      - エクスプローラーの上部にある「表示」メニューを確認
      - 「隠しファイルを表示」が有効になっているか確認
   3. `.env.example`を右クリック → 「Copy」を選択
   4. 同じ場所で右クリック → 「Paste」
   5. コピーされたファイル名を`.env.local`に変更

   **方法B: ターミナルでコピー**
   
   ターミナルで以下を実行：
   ```bash
   # Windowsの場合
   copy .env.example .env.local
   
   # Mac/Linuxの場合
   cp .env.example .env.local
   ```

   **方法C: 手動で作成**
   
   1. Cursorのエクスプローラーで、プロジェクトルートを右クリック
   2. 「新しいファイル」を選択
   3. ファイル名を`.env.local`と入力（先頭のドットを含む）
   4. 以下の内容をコピー＆ペースト：
   ```env
   # 環境変数のテンプレート
   # このファイルをコピーして .env.local を作成し、実際の値を設定してください

   # ============================================
   # 必須環境変数（Supabase）
   # ============================================
   # Supabaseのダッシュボード（Settings → API）から取得してください
   NEXT_PUBLIC_SUPABASE_URL=
   NEXT_PUBLIC_SUPABASE_ANON_KEY=
   SUPABASE_SERVICE_ROLE_KEY=

   # ============================================
   # オプション環境変数（メール機能）
   # ============================================
   # お問い合わせフォームやパスワードリセット機能を使用する場合に設定
   # EMAIL_SERVICE=resend
   # EMAIL_API_KEY=
   # EMAIL_FROM=
   # NEXT_PUBLIC_SITE_URL=

   # ============================================
   # オプション環境変数（AI要約機能）
   # ============================================
   # 学校のAI要約生成機能を使用する場合に設定
   # OPENAI_API_KEY=
   # OPENAI_MODEL=gpt-4o
   ```

3. **環境変数の値を設定**
   - `.env.local`ファイルを開く
   - オーナーから共有された環境変数の値を入力
   - ファイルを保存（Ctrl + S）

**重要**: `.env.local`ファイルは絶対にGitにコミットしないでください。

### ステップ3: 依存関係のインストール

ターミナルで以下を実行：

```bash
npm install
```

これには数分かかる場合があります。

### ステップ4: ローカルで起動確認

1. **開発サーバーを起動**
   ```bash
   npm run dev
   ```

2. **ブラウザで確認**
   - http://localhost:3000 を開く
   - サイトが表示されればOK

3. **サーバーを停止**
   - ターミナルで `Ctrl + C` を押す

## 日常的な作業フロー

### 1. タスクの確認

- GitHubのIssuesでタスクを確認
- または、オーナーから直接指示を受ける

### 2. ブランチの作成

ターミナルで以下を実行：

```bash
# devブランチに移動
git checkout dev

# 最新の状態を取得
git pull origin dev

# 新しいfeatureブランチを作成
git checkout -b feature/ui-xxx
```

**ブランチ名の例**:
- `feature/ui-school-header` - 学校ヘッダーの改善
- `feature/ui-button-color` - ボタンの色変更
- `feature/ui-text-spacing` - テキストの余白調整

### 3. Cursorで作業

詳細は「[Cursorでの作業手順](#cursorでの作業手順)」を参照。

### 4. ローカルで確認

1. **開発サーバーを起動**
   ```bash
   npm run dev
   ```

2. **ブラウザで確認**
   - http://localhost:3000 を開く
   - 変更内容が正しく反映されているか確認

3. **PC表示の確認**
   - デスクトップサイズで表示を確認
   - レイアウトが崩れていないか確認

4. **スマホ表示の確認**
   - ブラウザの開発者ツールを開く（F12）
   - デバイスツールバーを開く（Ctrl + Shift + M）
   - スマホサイズ（例: iPhone 12 Pro）に切り替えて確認

5. **Console errorの確認**
   - 開発者ツールの「Console」タブを開く
   - エラー（赤いメッセージ）がないか確認

6. **サーバーを停止**
   - ターミナルで `Ctrl + C` を押す

### 5. コミット・プッシュ

1. **変更内容を確認**
   ```bash
   git status
   ```
   変更されたファイルが表示されます

2. **変更をステージング**
   ```bash
   git add .
   ```
   または、特定のファイルのみ：
   ```bash
   git add app/page.tsx
   ```

3. **コミット**
   ```bash
   git commit -m "feat(ui): 変更内容の説明"
   ```
   コミットメッセージの例：
   - `feat(ui): トップページのヒーローセクションの余白を調整`
   - `feat(ui): ボタンの色を青から緑に変更`
   - `feat(ui): フォントサイズを大きく`

4. **プッシュ**
   ```bash
   git push -u origin feature/ui-xxx
   ```
   （`feature/ui-xxx`は実際のブランチ名に置き換えてください）

### 6. PRの作成

詳細は「[PR作成手順](#pr作成手順)」を参照。

## Cursorでの作業手順

### ステップ1: ファイルを開く

1. **Cursorでプロジェクトを開く**
   - 既に開いている場合はそのまま

2. **指定されたファイルを開く**
   - 左側のエクスプローラーでファイルをクリック
   - 例: `app/page.tsx`、`components/Header.tsx`

### ステップ2: Cursorに指示を渡す

1. **Cursorのチャットを開く**
   - `Ctrl + L` を押す
   - または、右側のチャットパネルを開く

2. **指示文を入力**
   - `CURSOR_WORK_TEMPLATE.md`のテンプレートを参考に
   - 変更したい内容を具体的に記述
   - 変更範囲を明確に指定

**指示文の例**:
```
app/page.tsxの309行目付近の「実際の体験談で」というテキストを「リアルな口コミで」に変更してください。

変更範囲: app/page.tsxの309行目のみ
禁止事項: 機能の変更、バックエンドの変更は禁止
```

### ステップ3: 変更内容の確認

1. **変更内容を確認**
   - Cursorが提案した変更を確認
   - 想定通りの変更か確認

2. **変更を適用**
   - 問題なければ変更を適用
   - 問題があれば修正を依頼

3. **Gitで変更内容を確認**
   ```bash
   git diff
   ```
   変更された行が表示されます。想定外の変更がないか確認してください。

### ステップ4: 変更範囲の確認

**重要**: 指定されたファイル以外を変更していないか確認：

```bash
git status
```

変更されたファイルの一覧が表示されます。指定されたファイル以外が変更されていないか確認してください。

## PR作成手順

### ステップ1: GitHubでPRを作成

1. **GitHubリポジトリのページにアクセス**
   - https://github.com/kurataraku/survey-app

2. **「Pull requests」タブをクリック**

3. **「New pull request」をクリック**

4. **ブランチを選択**
   - base: `dev` を選択
   - compare: `feature/ui-xxx` を選択（あなたが作成したブランチ）

5. **変更内容を確認**
   - 変更されたファイルが表示されます
   - 想定通りの変更か確認

### ステップ2: PR情報を入力

1. **タイトルを入力**
   - 例: `feat(ui): トップページのヒーローセクションの余白を調整`

2. **説明を記入**
   - PRテンプレートが自動的に表示されます
   - 各項目を記入：

   **変更内容**
   - 何を変更したか簡潔に記述
   - 例: 「トップページのヒーローセクションの余白を20pxから40pxに変更」

   **対象ページ / 画面**
   - 変更したページのURL
   - 例: `http://localhost:3000/`

   **変更ファイル**
   - 変更したファイル名
   - 例: `app/page.tsx`

   **スクショ（Before / After）**
   - Before: 変更前のスクリーンショットを添付
   - After: 変更後のスクリーンショットを添付
   - スクリーンショットの撮り方：
     1. ブラウザでページを開く
     2. `Win + Shift + S`（Windows）でスクリーンショットを撮る
     3. PRの説明欄に貼り付け

   **チェック**
   - すべての項目にチェックを入れる：
     - [x] 機能・バックエンド・サーバー側は変更していない
     - [x] PC表示OK
     - [x] スマホ表示OK
     - [x] Console errorなし
     - [x] 関係ないファイル変更が混ざっていない（git diff確認）
     - [x] ローカルで動作確認済み

### ステップ3: PRを提出

1. **「Create pull request」をクリック**

2. **PRが作成されたことを確認**
   - PR番号が表示されます（例: #2）

3. **Vercelプレビュー環境を確認**（任意）
   - PR作成後、Vercelが自動的にプレビュー環境をビルド
   - PRコメントに表示されるプレビューURLにアクセス
   - 変更内容が正しく反映されているか確認

### ステップ4: レビューを待つ

- オーナーがレビューを行います
- レビューコメントがあれば確認し、必要に応じて修正

## よくある質問とトラブルシューティング

### Gitコマンドがわからない

**Q: `git checkout`って何？**

A: ブランチを切り替えるコマンドです。`git checkout dev`で`dev`ブランチに切り替わります。

**Q: `git pull`って何？**

A: リモートリポジトリから最新の変更を取得するコマンドです。他の人が変更を加えた場合、最新の状態に更新するために使います。

**Q: `git push`って何？**

A: ローカルの変更をリモートリポジトリ（GitHub）にアップロードするコマンドです。

### ブランチの切り替え方法

```bash
# 現在のブランチを確認
git branch

# ブランチを切り替え
git checkout ブランチ名

# 例: devブランチに切り替え
git checkout dev
```

### コンフリクトの解決方法

**Q: コンフリクトが発生した**

A: 他の人が同じファイルを変更した場合、コンフリクトが発生することがあります。

1. オーナーに連絡
2. オーナーの指示に従って解決
3. 通常は、最新の`dev`ブランチを取得してから再度作業を開始

### PRがマージされない理由

**Q: PRがマージされない**

考えられる理由：

1. **レビューがまだ完了していない**
   - オーナーがレビュー中です。しばらくお待ちください。

2. **修正依頼がある**
   - PRにコメントがついているか確認
   - 修正が必要な場合は対応してください

3. **チェックリストが完了していない**
   - PRの説明欄のチェックリストを確認
   - すべてチェックが入っているか確認

### プレビュー環境の確認方法

1. **PRページを開く**
2. **コメント欄を確認**
   - Vercelからの自動コメントにプレビューURLが表示されます
3. **プレビューURLにアクセス**
   - 変更内容が正しく反映されているか確認

### 環境変数のエラー

**Q: `環境変数が設定されていません`というエラーが出る**

A: `.env.local`ファイルが正しく作成され、環境変数が設定されているか確認してください。

1. `.env.local`ファイルが存在するか確認
2. 環境変数の値が正しく設定されているか確認
3. 開発サーバーを再起動（`Ctrl + C`で停止してから`npm run dev`で再起動）

### 変更が反映されない

**Q: 変更したのにブラウザに反映されない**

A: 以下を試してください：

1. **ブラウザをリロード**
   - `Ctrl + R` または `F5`

2. **ハードリロード**
   - `Ctrl + Shift + R` または `Ctrl + F5`

3. **開発サーバーを再起動**
   - ターミナルで `Ctrl + C` で停止
   - `npm run dev` で再起動

4. **キャッシュをクリア**
   - ブラウザのキャッシュをクリア

### npmコマンドが認識されない

**Q: `'npm' は、内部コマンドまたは外部コマンド、操作可能なプログラムまたはバッチ ファイルとして認識されていません。`というエラーが出る**

A: Node.jsがインストールされていないか、PATHが正しく設定されていない可能性があります。

**解決方法1: Node.jsがインストールされているか確認**

1. **ターミナルで確認**
   ```bash
   node --version
   ```
   - バージョンが表示されれば、Node.jsはインストールされています
   - エラーが出る場合は、Node.jsがインストールされていません

2. **Node.jsをインストール**
   - https://nodejs.org/ にアクセス
   - LTS版（推奨版）をダウンロード
   - インストーラーを実行
   - インストール後、**ターミナルを再起動**（重要）

3. **インストールを確認**
   ```bash
   node --version
   npm --version
   ```
   両方のコマンドでバージョンが表示されればOK

**解決方法2: PATHが正しく設定されているか確認**

1. **Node.jsのインストールパスを確認**
   - 通常は `C:\Program Files\nodejs\` にインストールされます

2. **環境変数のPATHを確認**
   - Windowsキー + R → `sysdm.cpl` と入力
   - 「詳細設定」タブ → 「環境変数」をクリック
   - 「システム環境変数」の「Path」を選択 → 「編集」
   - `C:\Program Files\nodejs\` が含まれているか確認
   - 含まれていない場合は追加

3. **ターミナルを再起動**
   - 環境変数を変更した後は、ターミナルを再起動する必要があります

**解決方法3: Cursorのターミナルを使用**

1. Cursorのターミナルを使用してみる
   - Cursorのメニューから「Terminal」→「New Terminal」
   - または、`Ctrl + ``（バッククォート）でターミナルを開く

2. 再度`npm install`を実行

**解決方法4: フルパスで実行（一時的な対処）**

Node.jsがインストールされている場合：
```bash
"C:\Program Files\nodejs\npm.cmd" install
```

### ファイルが見つからない

**Q: `.env.example`ファイルが見つからない**

A: `.env.example`は隠しファイルとして扱われる場合があります。以下の方法を試してください：

1. **Cursorのエクスプローラーで隠しファイルを表示**
   - エクスプローラーの上部にある「表示」メニューを確認
   - 「隠しファイルを表示」が有効になっているか確認
   - または、設定で「Show Hidden Files」を有効にする

2. **ターミナルで確認**
   ```bash
   # Windowsの場合
   dir /a .env.example
   
   # Mac/Linuxの場合
   ls -la .env.example
   ```
   ファイルが存在するか確認できます

3. **ターミナルで直接コピー**
   ```bash
   # Windowsの場合
   copy .env.example .env.local
   
   # Mac/Linuxの場合
   cp .env.example .env.local
   ```

4. **手動で作成**
   - 上記の「方法C: 手動で作成」を参照
   - `.env.local`ファイルを直接作成して、環境変数の値を入力

**Q: `.env.local`ファイルが見つからない**

A: 隠しファイルとして扱われている可能性があります。

1. Cursorのエクスプローラーで「表示」→「隠しファイルを表示」を確認
2. ファイル名が正確に`.env.local`になっているか確認（先頭のドットを含む）

## 参考情報

- `CURSOR_WORK_TEMPLATE.md` - Cursor作業用テンプレート
- `README.md` - プロジェクトの概要
- `.github/pull_request_template.md` - PRテンプレート

## サポート

作業中に問題が発生した場合：

1. このガイドの「よくある質問とトラブルシューティング」を確認
2. 解決しない場合は、オーナーに連絡

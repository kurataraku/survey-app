# Cursor作業用テンプレート

このテンプレートを参考に、Cursorに指示を渡してください。

## 基本的な使い方

1. Cursorのチャットを開く（`Ctrl + L`）
2. 以下のテンプレートをコピー
3. 具体的な内容を記入
4. Cursorに送信

## テンプレート

```
目的：UI改善のみ。フロントエンドの見た目・文言・デザインの改善を行う。
変更範囲：指定ファイルのみ。指定外ファイルは編集しない。

✅ 許可される変更：
- 文言・テキストの変更
- 文字サイズ・フォント・色の変更
- トーン&マナーの調整
- 画像の挿入・差し替え
- レイアウト・余白・サイズの調整
- スタイリング（CSS/Tailwind）の変更

❌ 絶対に禁止される変更：
- 機能の追加・変更・削除
- バックエンド（API、データベース）への変更
- サーバー側（API Route、Server Component）のロジック変更
- データ取得ロジックの変更
- ルーティングの変更
- 認証・セキュリティ関連の変更
- 環境変数の追加・変更
- パッケージの追加・削除
- 不要なリファクタリング
- import整理だけの変更

【具体的な変更内容】
[ここに変更したい内容を具体的に記述]

【変更対象ファイル】
[例: app/page.tsx の309行目]

【変更前】
[現在の状態を記述]

【変更後】
[変更後の状態を記述]

作業後：git diffを見て想定外の変更がないことを確認し、Before/Afterスクショを用意する。
```

## 使用例

### 例1: 文言の変更

```
目的：UI改善のみ。フロントエンドの見た目・文言・デザインの改善を行う。
変更範囲：指定ファイルのみ。指定外ファイルは編集しない。

✅ 許可される変更：
- 文言・テキストの変更
- 文字サイズ・フォント・色の変更
- トーン&マナーの調整
- 画像の挿入・差し替え
- レイアウト・余白・サイズの調整
- スタイリング（CSS/Tailwind）の変更

❌ 絶対に禁止される変更：
- 機能の追加・変更・削除
- バックエンド（API、データベース）への変更
- サーバー側（API Route、Server Component）のロジック変更
- データ取得ロジックの変更
- ルーティングの変更
- 認証・セキュリティ関連の変更
- 環境変数の追加・変更
- パッケージの追加・削除
- 不要なリファクタリング
- import整理だけの変更

【具体的な変更内容】
トップページのヒーローセクションの文言を変更したい

【変更対象ファイル】
app/page.tsx の309行目

【変更前】
実際の体験談で

【変更後】
リアルな口コミで

作業後：git diffを見て想定外の変更がないことを確認し、Before/Afterスクショを用意する。
```

### 例2: 余白の調整

```
目的：UI改善のみ。フロントエンドの見た目・文言・デザインの改善を行う。
変更範囲：指定ファイルのみ。指定外ファイルは編集しない。

✅ 許可される変更：
- 文言・テキストの変更
- 文字サイズ・フォント・色の変更
- トーン&マナーの調整
- 画像の挿入・差し替え
- レイアウト・余白・サイズの調整
- スタイリング（CSS/Tailwind）の変更

❌ 絶対に禁止される変更：
- 機能の追加・変更・削除
- バックエンド（API、データベース）への変更
- サーバー側（API Route、Server Component）のロジック変更
- データ取得ロジックの変更
- ルーティングの変更
- 認証・セキュリティ関連の変更
- 環境変数の追加・変更
- パッケージの追加・削除
- 不要なリファクタリング
- import整理だけの変更

【具体的な変更内容】
トップページのヒーローセクションの余白を調整したい

【変更対象ファイル】
app/page.tsx の306-313行目付近

【変更前】
<div className="text-center mb-8">

【変更後】
<div className="text-center mb-12">

作業後：git diffを見て想定外の変更がないことを確認し、Before/Afterスクショを用意する。
```

### 例3: 色の変更

```
目的：UI改善のみ。フロントエンドの見た目・文言・デザインの改善を行う。
変更範囲：指定ファイルのみ。指定外ファイルは編集しない。

✅ 許可される変更：
- 文言・テキストの変更
- 文字サイズ・フォント・色の変更
- トーン&マナーの調整
- 画像の挿入・差し替え
- レイアウト・余白・サイズの調整
- スタイリング（CSS/Tailwind）の変更

❌ 絶対に禁止される変更：
- 機能の追加・変更・削除
- バックエンド（API、データベース）への変更
- サーバー側（API Route、Server Component）のロジック変更
- データ取得ロジックの変更
- ルーティングの変更
- 認証・セキュリティ関連の変更
- 環境変数の追加・変更
- パッケージの追加・削除
- 不要なリファクタリング
- import整理だけの変更

【具体的な変更内容】
ボタンの色を青から緑に変更したい

【変更対象ファイル】
components/Button.tsx の15行目付近

【変更前】
className="bg-blue-500 hover:bg-blue-600"

【変更後】
className="bg-green-500 hover:bg-green-600"

作業後：git diffを見て想定外の変更がないことを確認し、Before/Afterスクショを用意する。
```

## 重要なポイント

### 変更範囲を明確に指定

- ファイル名と行番号を指定
- 例: `app/page.tsx の309行目`

### 変更内容を具体的に記述

- 「余白を調整」ではなく「余白を20pxから40pxに変更」
- 「色を変更」ではなく「色を青から緑に変更」

### 禁止事項を再確認

- テンプレートの禁止事項を必ず含める
- Cursorが意図しない変更をしないようにする

### 作業後の確認

- `git diff`で変更内容を確認
- 想定外の変更がないか確認
- Before/Afterのスクショを用意

## トラブルシューティング

### Cursorが意図しない変更をした場合

1. **変更を確認**
   ```bash
   git diff
   ```

2. **問題があれば元に戻す**
   ```bash
   git restore ファイル名
   ```

3. **再度指示を渡す**
   - より具体的に指示を記述
   - 変更範囲をより明確に指定

### 複数のファイルが変更された場合

1. **変更内容を確認**
   ```bash
   git status
   ```

2. **指定したファイル以外を元に戻す**
   ```bash
   git restore ファイル名
   ```

3. **指定したファイルのみをコミット**
   ```bash
   git add 指定したファイル名
   git commit -m "feat(ui): 変更内容の説明"
   ```

## 参考情報

- `MEMBER_GUIDE.md` - メンバー向けの完全ガイド
- `README.md` - プロジェクトの概要

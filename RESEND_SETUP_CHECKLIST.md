# Resend設定チェックリスト

管理者ユーザー追加時のメール送信機能を動作させるためのResend設定確認チェックリストです。

## 📋 チェック項目

### 1. Resendアカウントの確認

- [ ] Resendアカウントにログインできる
- [ ] アカウントのメールアドレスを確認
  - **重要**: `onboarding@resend.dev`を使用する場合、このメールアドレスにのみ送信可能

### 2. APIキーの確認

- [ ] Resendダッシュボードで「API Keys」セクションを開く
- [ ] 有効なAPIキーが存在することを確認
- [ ] APIキーの形式が `re_` で始まることを確認
- [ ] APIキーが無効化されていないことを確認
- [ ] `.env.local` の `EMAIL_API_KEY` に正しいAPIキーが設定されている

**確認方法:**
```bash
# .env.localの内容を確認（実際の値は表示されませんが、設定されているか確認）
echo $EMAIL_API_KEY  # または .env.localファイルを直接確認
```

### 3. 環境変数の確認

`.env.local` ファイルに以下の変数が正しく設定されているか確認：

- [ ] `EMAIL_SERVICE=resend`
- [ ] `EMAIL_API_KEY=re_xxxxxxxxxxxxx`（実際のAPIキー）
- [ ] `EMAIL_FROM=...`（送信元メールアドレス）

**開発環境の場合:**
```env
EMAIL_SERVICE=resend
EMAIL_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM=onboarding@resend.dev
```

**本番環境の場合:**
```env
EMAIL_SERVICE=resend
EMAIL_API_KEY=re_xxxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com  # 検証済みドメインのメールアドレス
```

### 4. ドメインの設定確認

#### 開発環境（`onboarding@resend.dev`を使用する場合）

- [ ] `EMAIL_FROM=onboarding@resend.dev` が設定されている
- [ ] **重要**: 送信先メールアドレスが**Resendアカウントのメールアドレス**であることを確認
  - `onboarding@resend.dev`は、Resendアカウントのメールアドレスにのみ送信可能
  - 他のメールアドレスに送信しようとすると403エラーが発生

#### 本番環境（カスタムドメインを使用する場合）

- [ ] Resendダッシュボードで「Domains」セクションを開く
- [ ] 使用するドメインが追加されている
- [ ] ドメインのステータスが「Verified」であることを確認
- [ ] DNSレコード（TXT、CNAME）が正しく設定されている
- [ ] `EMAIL_FROM`に検証済みドメインのメールアドレスが設定されている

**DNSレコードの確認:**
- ドメイン管理画面でTXTレコードとCNAMEレコードが正しく設定されているか確認
- Resendダッシュボードで「Verify」ボタンをクリックして検証

### 5. 開発サーバーの再起動

環境変数を変更した場合：

- [ ] 開発サーバーを停止（`Ctrl + C`）
- [ ] 開発サーバーを再起動（`npm run dev`）
- [ ] 環境変数が正しく読み込まれていることを確認

### 6. メール送信テスト

管理者ユーザーを追加してメール送信をテスト：

1. **テスト手順:**
   - [ ] `/admin/admin-users` にアクセス
   - [ ] 管理者ユーザーを追加
     - `onboarding@resend.dev`を使用する場合：**Resendアカウントのメールアドレス**を入力
     - 検証済みドメインを使用する場合：任意のメールアドレスを入力

2. **サーバーログの確認:**
   - [ ] 開発サーバーのコンソールで以下のログを確認：
     - `[Email] Resend API リクエスト送信（パスワードリセット）`
     - `[Email] Resend API レスポンス（パスワードリセット）`
     - エラーメッセージがないことを確認

3. **Resendダッシュボードでの確認:**
   - [ ] Resendダッシュボードの「Logs」セクションを開く
   - [ ] 送信履歴を確認
   - [ ] エラーがないことを確認
   - [ ] 送信ステータスが「Delivered」または「Sent」であることを確認

4. **メール受信の確認:**
   - [ ] 送信先メールアドレスの受信トレイを確認
   - [ ] 迷惑メールフォルダも確認
   - [ ] メールが届いていることを確認

### 7. エラーの確認

以下のエラーが発生した場合の対処：

#### `Resend API error (403)`
**原因:**
- `onboarding@resend.dev`を使用している場合、Resendアカウントのメールアドレス以外に送信しようとしている
- カスタムドメインが検証されていない

**対処方法:**
- 開発環境：送信先をResendアカウントのメールアドレスに変更
- 本番環境：ドメインを検証するか、`onboarding@resend.dev`を使用してResendアカウントのメールアドレスに送信

#### `Resend API error (401)`
**原因:** APIキーが無効または間違っている

**対処方法:**
- Resendダッシュボードで新しいAPIキーを生成
- `.env.local`の`EMAIL_API_KEY`を更新
- 開発サーバーを再起動

#### `Resend API error (422)`
**原因:** 
- 無効なメールアドレス形式
- ドメインが検証されていない（カスタムドメインの場合）

**対処方法:**
- メールアドレスの形式を確認
- カスタムドメインの検証状況を確認

#### `メール送信APIキー（EMAIL_API_KEY）が設定されていません`
**原因:** `.env.local`に`EMAIL_API_KEY`が設定されていない

**対処方法:**
- `.env.local`に`EMAIL_API_KEY`を追加
- 開発サーバーを再起動

#### `送信元メールアドレス（EMAIL_FROM）が設定されていません`
**原因:** `.env.local`に`EMAIL_FROM`が設定されていない

**対処方法:**
- `.env.local`に`EMAIL_FROM`を追加
- 開発サーバーを再起動

### 8. トラブルシューティング

問題が解決しない場合：

1. **ログの詳細確認:**
   - [ ] 開発サーバーのコンソールで詳細なエラーメッセージを確認
   - [ ] Resendダッシュボードの「Logs」セクションでエラーの詳細を確認

2. **環境変数の再確認:**
   - [ ] `.env.local`ファイルの内容を再度確認
   - [ ] スペルミスや余分なスペースがないか確認
   - [ ] 値が正しく設定されているか確認（APIキーの先頭に`re_`があるかなど）

3. **APIキーの再生成:**
   - [ ] Resendダッシュボードで古いAPIキーを無効化
   - [ ] 新しいAPIキーを生成
   - [ ] `.env.local`を更新
   - [ ] 開発サーバーを再起動

4. **ドメインの再検証:**
   - [ ] DNSレコードが正しく設定されているか再確認
   - [ ] Resendダッシュボードで「Verify」を再度実行

## ✅ チェックリスト完了後

すべてのチェック項目を確認したら：

1. 管理者ユーザーを追加してメール送信をテスト
2. メールが正しく送信されることを確認
3. メールが受信されることを確認

## 📝 メモ

- `onboarding@resend.dev`は開発環境でのみ使用を推奨
- 本番環境ではカスタムドメインの検証を推奨
- APIキーは機密情報のため、`.gitignore`に含まれていることを確認

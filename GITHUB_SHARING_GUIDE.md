# GitHubリポジトリ共有手順（オーナー向け）

このガイドは、GitHubリポジトリをメンバーと共有する手順を説明します。

## 前提条件

- GitHubアカウントを持っていること
- リポジトリのオーナー権限を持っていること
- メンバーのGitHubアカウント情報（ユーザー名またはメールアドレス）を把握していること

## ステップ1: メンバーをCollaboratorに追加

### 1-1. リポジトリの設定ページにアクセス

1. GitHubリポジトリのページにアクセス
   - https://github.com/kurataraku/survey-app

2. 「Settings」タブをクリック

3. 左メニューから「Collaborators」を選択
   - 「Access」セクションの下にあります

### 1-2. メンバーを追加

1. 「Add people」ボタンをクリック

2. メンバーの情報を入力
   - GitHubユーザー名を入力
   - または、メールアドレスを入力（GitHubアカウントに紐づいている場合）

3. 権限を選択
   - **推奨**: 「Write」権限を選択
     - ブランチの作成・プッシュが可能
     - PRの作成が可能
     - `main`ブランチへの直接pushは保護ルールで禁止されているため安全

4. 「Add [ユーザー名] to this repository」をクリック

### 1-3. 招待の送信

- 招待が送信されると、メンバーにメール通知が届きます
- メンバーが招待を承認すると、リポジトリへのアクセスが可能になります

## ステップ2: メンバーに初回セットアップを案内

メンバーが招待を承認したら、以下の情報を共有してください：

### 共有する情報

1. **リポジトリURL**
   ```
   https://github.com/kurataraku/survey-app
   ```

2. **メンバー向けガイド**
   - `MEMBER_GUIDE.md`を参照するよう案内
   - または、直接ガイドの内容を共有

3. **環境変数の値**（機密情報）
   - `.env.local`に設定する必要がある環境変数の値を共有
   - **重要**: メールやチャットで共有する場合は、セキュアな方法を使用
   - 推奨: 1Password、LastPassなどのパスワード管理ツールを使用

### 環境変数の共有方法

以下の環境変数の値をメンバーに共有してください：

```env
NEXT_PUBLIC_SUPABASE_URL=実際の値
NEXT_PUBLIC_SUPABASE_ANON_KEY=実際の値
SUPABASE_SERVICE_ROLE_KEY=実際の値
```

**注意**: `SUPABASE_SERVICE_ROLE_KEY`は機密情報です。安全な方法で共有してください。

## ステップ3: 初回セットアップのサポート

メンバーが初回セットアップで困っている場合のサポート手順：

### よくある問題

1. **リポジトリのクローンができない**
   - Gitがインストールされているか確認
   - GitHubアカウントでログインしているか確認

2. **環境変数の設定がわからない**
   - `MEMBER_GUIDE.md`の「初回セットアップ」セクションを案内
   - `.env.example`ファイルの存在を確認

3. **`npm install`でエラーが出る**
   - Node.jsのバージョンを確認（推奨: v18以上）
   - `package-lock.json`が存在するか確認

### サポート時の確認事項

- [ ] GitHubアカウントでログインできているか
- [ ] リポジトリへのアクセス権限があるか
- [ ] Gitがインストールされているか
- [ ] Node.jsがインストールされているか
- [ ] `.env.local`ファイルが正しく作成されているか
- [ ] 環境変数の値が正しく設定されているか

## ステップ4: メンバーの権限を変更する

### 4-1. 招待が承認されている場合（通常の権限変更）

メンバーが招待を承認している場合：

1. 「Settings」→「Collaborators」に移動
2. メンバー一覧で、権限を変更したいメンバーを選択（チェックボックスにチェック）
3. 上部の「Type」ドロップダウンをクリック
4. 希望する権限を選択
   - **Read**: 読み取りのみ（推奨しない）
   - **Triage**: IssuesやPRの管理が可能
   - **Write**: ブランチの作成・プッシュ、PRの作成が可能（**推奨**）
   - **Maintain**: リポジトリの管理が可能（通常は不要）
   - **Admin**: 管理者権限（通常は不要）
5. 変更が自動的に保存されます

### 4-2. 招待が保留中（Pending Invite）の場合

「Awaiting [ユーザー名]'s response」と表示されている場合、権限を変更できません。

**方法1: 招待をキャンセルして再招待（権限を変更したい場合）**

1. メンバー一覧で、保留中のメンバーを選択
2. 右端のゴミ箱アイコン（Delete invitation）をクリック
3. 招待をキャンセルすることを確認
4. 「Add people」ボタンをクリック
5. 再度メンバーを追加する際に、希望する権限（Write）を選択
6. 招待を送信

**方法2: 招待が承認されるのを待つ**

- メンバーが招待を承認すると、自動的に設定した権限が適用されます
- 承認後、必要に応じて上記「4-1」の手順で権限を変更できます

## ステップ5: 権限の確認

メンバーが正しく追加されているか確認：

1. 「Settings」→「Collaborators」に移動
2. メンバー一覧を確認
3. 各メンバーの権限を確認（「Write」になっているか）
4. 「Pending Invite」状態のメンバーがいないか確認

## トラブルシューティング

### メンバーが招待メールを受け取れない場合

1. メンバーのメールアドレスを確認
2. スパムフォルダを確認
3. 手動で招待URLを共有
   - 「Settings」→「Collaborators」で招待URLをコピー

### メンバーがリポジトリにアクセスできない場合

1. メンバーが招待を承認しているか確認
2. メンバーのGitHubアカウントでログインしているか確認
3. 権限設定を再確認

### メンバーがブランチを作成できない場合

1. 権限が「Write」以上になっているか確認
2. ブランチ保護ルールが影響していないか確認
   - `main`ブランチへの直接pushは禁止されているが、`feature/*`ブランチの作成は可能

### メンバーの権限を変更できない場合

**Q: 「Pending Invite」状態で権限を変更できない**

A: 招待が保留中の場合は権限を変更できません。以下のいずれかを実行してください：

1. **招待をキャンセルして再招待**
   - ゴミ箱アイコンをクリックして招待をキャンセル
   - 再招待時に希望する権限を設定

2. **招待が承認されるのを待つ**
   - メンバーが招待を承認すると、設定した権限が適用されます
   - 承認後、必要に応じて権限を変更できます

## 次のステップ

メンバーが招待を承認したら：

1. `MEMBER_GUIDE.md`を共有
2. 初回セットアップのサポート
3. 最初のタスクを割り当て（小さなタスクから開始）

## 参考情報

- [GitHub公式ドキュメント: Collaborators](https://docs.github.com/ja/repositories/managing-your-repositorys-settings-and-features/managing-repository-settings/managing-teams-and-people-with-access-to-your-repository)
- `MEMBER_GUIDE.md` - メンバー向けの完全ガイド
